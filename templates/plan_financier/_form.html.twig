<div class="container" style="margin-left: 40px;">
    <div class="row">
        {{ form_start(form, {
            'attr': {
                'novalidate': 'novalidate',
                'class': 'col-lg-8'
            }
        })
        }}

        {{ form_row(form.motif) }}
        {{ form_row(form.montant) }}
        <button type="submit" class="btn btn-primary">Enregistrer</button>
        <a href="{{ path('app_plan_financier_index') }}" class="btn btn-success">Retour à la liste</a>
        {{ form_end(form) }}
    </div>
</div>

