{% extends 'base.html.twig' %}

{% block title %}PlanCommunication{% endblock %}

{% block body %}
    <h1>PlanCommunication</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ plan_communication.id }}</td>
            </tr>
            <tr>
                <th>Objectif</th>
                <td>{{ plan_communication.objectif }}</td>
            </tr>
            <tr>
                <th>Cible</th>
                <td>{{ plan_communication.cible }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_plan_communication_index') }}">back to list</a>

    <a href="{{ path('app_plan_communication_edit', {'id': plan_communication.id}) }}">edit</a>

    {{ include('plan_communication/_delete_form.html.twig') }}
{% endblock %}
