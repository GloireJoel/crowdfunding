<form method="post" action="{{ path('app_plan_communication_delete', {'id': plan_communication.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plan_communication.id) }}">
    <button class="btn">Delete</button>
</form>
